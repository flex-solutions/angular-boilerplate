// Define constant mapping with controller in server side
export enum ControllerConstant {
  Account = 'account'
}

// Define constant mapping with api in server side
export enum ApiConstant {
  login = 'login',
  logout = 'logout',
  get = 'get',
  getAll = 'getall'
}
