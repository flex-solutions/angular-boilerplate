import { NewsStatusDirective } from './news-status.directive';

describe('NewsStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new NewsStatusDirective();
    expect(directive).toBeTruthy();
  });
});
